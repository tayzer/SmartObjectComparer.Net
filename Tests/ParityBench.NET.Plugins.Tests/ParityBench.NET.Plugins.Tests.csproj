<Project Sdk="MSTest.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\..\Source\ParityBench.NET.Plugins\ParityBench.NET.Plugins.csproj" />
    <!--
      Referenced only so the fixture package is built before these tests run; the
      tests load it from disk through the plugin loader, never as a reference.
    -->
    <ProjectReference Include="..\Fixtures\ParityBench.TestPlugin\ParityBench.TestPlugin.csproj">
      <ReferenceOutputAssembly>false</ReferenceOutputAssembly>
    </ProjectReference>
    <!--
      A second build of the same fixture source, so the reload tests can overwrite an
      installed package with different bytes under an unchanged version.
    -->
    <ProjectReference Include="..\Fixtures\ParityBench.TestPlugin.Rebuilt\ParityBench.TestPlugin.Rebuilt.csproj">
      <ReferenceOutputAssembly>false</ReferenceOutputAssembly>
    </ProjectReference>
  </ItemGroup>

</Project>
